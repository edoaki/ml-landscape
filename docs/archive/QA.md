# 制作・検証記録

> 2026-09-22: 開発環境はNode.js＋npmへ移行済みです。以下のPythonコマンド・ファイル名は過去の制作記録で、旧コードはリポジトリの `docs/legacy-python/` に保管しています。現在の手順はプロジェクトREADMEを参照してください。

2026-09-20の再構成・移行の検証は[今回の移行記録](migration-report.md)と[ブラウザ検証結果](../verification/restructure-2026-09-20/browser-results.json)を参照。以下は日付ごとの過去の記録であり、現在のページ構成や今回の検証結果を表すものではありません。

確認日：2026-09-12

## 完成した範囲

- 静的HTML 26ページ。本文は約5.5万字（図中文字・数式・表を含む抽出文字数）。127節を本文検索から参照可能。
- 章の案内→学習の枠組み→モデル10系列→タスクと実問題→横断研究→研究13例。古典手法は任意の別ページ。
- GCN・GAT・GraphSAGEの独立節と同じ6ノードでの集約計算。GCNとGATは自己ループを表示し、GATは固定したW・aからスコアとsoftmaxも数値計算。
- VLMは独立節と初代LLaVAの構造図。「画像と言語の照合・応答」のタスク紹介を維持。
- XAI。Shapley・寄与度分解・IG、画像4手法の実計算結果、反実仮想2例、LLM3例。基準・計算条件・内部への介入を明示。
- 蒸留・量子化・LoRA、汎化・最適化・頑健性・不確実性と校正。
- 著者公開の画像・予測結果・映像、合成音、AlphaFold DBのP69905の座標とpLDDTに基づく回転表示。
- 共通ポータルの06を公開状態にし、静的ページ・素材の配信へ接続。Pythonを起動せず`index.html`を直接開く使い方も維持。

## 内容・計算の確認

- 畳み込み：4×4と2×2から3×3出力を計算。操作位置と積和の数値が一致。
- Attention：スコア[0,s,0]のsoftmaxとVの加重和が一致。因果マスクで参照禁止位置の係数が0。
- GCN：自己ループ後の次数[4,2,3,3,2,2]と対称正規化の係数。Aの更新は約(1.116,1.284)。
- GAT：W=I、a=[0,0,1,0]の例でスコア[1,0,2,1]。Aの更新は約(1.462,0.876)。
- GraphSAGE：B・Cの平均(1,1.5)、自己特徴との連結、例示用の線形変換とL2正規化で(0.8,0.6)。
- Flow Matching：直線経路の位置と速度を分け、時刻の向きをDDPMと明示的に区別。
- 校正：固定した正解頻度と、操作する確信度のずれを計算。100%でのクリップを含む。
- Grad-CAM：原論文Figure 1のcat/dogを切り替え、Grad-CAMと画素差分の意味を区別。
- AlphaFold：142個のCα座標、pLDDT範囲65.38–98.88を確認。回転は座標変換のみ。
- 公開結果と模式図・合成データをキャプションで分離。論文図の対象モデル・図番号が分かる場合は記載。

## 自動検査・ブラウザ確認

### 標準ライブラリのテスト

```sh
python3 -m unittest discover -s tests -p 'test_landscape.py' -v
node tests/test_ssm_animation.cjs
```

9テスト成功。ローカルリンク・節アンカー・重複ID・素材の存在・SVGのXML・構造データを確認。ポータルでは章一覧、既存marimoの起動・再利用・終了、06でプロセスを起動しないこと、静的配信、音声のRange応答、章外や制作ソースへのアクセス拒否を確認。

### Chromiumでの表示と操作

Playwright + Chromiumで確認（制作環境の実行補助を使用し、教材側に依存関係は追加していない）。

- 全26ページを1440×1000と390×844で巡回：ページ全体の横はみ出し、画像切れ、節リンクの欠落、JavaScriptエラーなし。
- 全SVGの文字のviewBox外へのはみ出しなし。
- 導入、モデル一覧、GAT、XAI、研究例をスクリーンショットで目視確認。日本語見出し・図・キャプション・サイドバーを確認。
- スマートフォンでは目次ボタン、開閉、Escape、検索を確認。広い図は文字を潰さず横スクロール。写真・論文の結果図には拡大リンクを用意。
- 本文検索、検索結果から節への移動、Escapeでの検索終了、畳み込み、Attentionと因果マスク、GNN、Flow Matching、校正、Grad-CAMの切り替えを確認。
- NeRFとRT-2のローカル動画の再生、3つの音声の再生、AlphaFold構造の回転を確認。
- 共通教材一覧→06→GNNの移動を確認。
- `file://`で直接開いたときの検索と、JavaScript無効時の本文・静止図の存在を確認。

## 導入の改訂（2026-09-12）

- 読者の指摘により、入口の五軸によるMNIST整理・同じ写真の出力比較・全体のつながりを削除。章の案内と読み進め方に整理。
- 全26ページから「このページの要点」を削除し、目次・検索にも残らないよう再生成。
- 学習方法の説明を、定義→具体例→学習で行うことの順に改訂。メール分類、購入傾向のグループ分け、迷路の報酬を例にし、抽象的なフロー図と数式を削除。
- 自己教師あり学習を説明した上で、文章の事前学習→質問と回答による追加学習という一例で組み合わせを説明。
- 削除した導入を参照していた研究例の文章も修正。
- 改訂後にリンク・SVGの既存2テスト成功。入口と学習方法のページをPC・スマートフォン幅で表示し、横はみ出し・JavaScriptエラーなし。全26ページと検索からの要点欄の削除、改訂本文のオフライン検索、ポータルからの移動を確認。

## CNNの改訂（2026-09-12）

- 全体図を特徴マップの重なり・縮小・Flatten・全結合・分類出力が見える独自の図へ変更。U-Netを最下段込み3段の解像度（縮小2回・拡大2回）へ変更。
- 畳み込みの入力・フィルタ・出力を並べ、操作位置と出力のマスを対応付けた。初期値はユーザー確認済みのstride=1・padding=0で、4×4入力→3×3出力。
- padding=0/1、stride=1/2/3、poolingなし/最大値/平均値を操作できる。poolingは2×2・stride=2、ReLUなし。端の切り捨てと適用できない1×1出力も説明。
- 6通りのstride/paddingを手計算の期待値と照合し、全出力位置の4画素の選択、最大値・平均値、poolingの元領域、出力マスからの選択をブラウザで確認。エラーなし。
- 新しい図・操作部をPCとスマートフォン幅で確認。図中文字のはみ出しなし。ローカルリンク・SVGの既存2テスト成功。

## 制作中に修正した点

- スマートフォンのヘッダーボタンの縦折り返しを修正。
- SVGを縮小しすぎて文字が小さくなる箇所を、横スクロールへ変更。
- SVGの矢印マーカーIDを各図で一意にし、複数図での衝突を回避。
- 検索入力のEscapeが検索欄に消費される場合も、検索ダイアログが閉じるよう明示処理。
- GATは任意に与えた係数だけでなく、例示用パラメータからスコアと係数を追える数値へ変更。
- 動画の取得先と内容を確認。NeRFは著者ページ掲載の大学サーバ、RT-2は説明アニメではなく操作例のteaser映像を採用。
- メディアの途中読み出しに対応し、移動・シークによる接続中断を正常に扱うようにした。

## 未実施の確認

- B3の読者による理解・つまずき・各ページの読了時間の試読。固定時間に収める省略はしていない。
- Windows・Safari・Firefoxの実機確認。教材は標準HTML/CSS/SVG/JavaScriptを使用。
- 外部のSAM等の操作デモでファイルを送信して推論する操作、ログイン後の画面、待ち時間の測定。教材内の閲覧・操作はそれらに依存しない。
- OpenReview上の指定論文PDFはブラウザ確認画面により取得できず、著者の2021年発表資料と公開実装で照合。本文・素材記録にこの区別を記載。

学習者向けの説明は`docs/`に依存しない。論文を選ぶパートや研究の自己分類の課題は含めていない。

### RNN・LSTM・GRUの説明更新（2026-09-12）

- 文脈が必要な具体例から状態・時間方向の重み共有へ進む導入を追加し、RNN・LSTM・GRUそれぞれに設計思想を1文で明示。
- 参考画像に沿ってループと時間展開、1ステップの計算、LSTMのセル経路、GRUのreset/update経路を新規SVG化。GRUのzは過去を残す割合で統一。
- 全26ページの再生成成功。`python3 -m unittest discover -s tests -p 'test_landscape.py'` の2件が成功（ローカルリンク・素材・ID・SVG等）。
- ローカル配信ページをブラウザで開き、導入と目次の遷移、RNN・LSTM・GRUの図、数式を確認。ゲート名と線の重なり、およびGRUのresetへの入力と候補への出力が重なる箇所を修正後、再確認。

## CNNを使う理由とラベル間隔の改訂（2026-09-12）

- CNNもNNの一種であることを明記し、全結合のMLPに対する局所接続・重み共有・パラメータ数の違いを説明。4×4入力→9出力の1層を144個と4個の重みで比較する独自図を追加。バイアスは除外し、性能が同じという比較にはしていない。
- 全体図のFlatten下のラベルを2行に分け、全結合下のラベルとの間隔を拡大。追加・修正した2図の全テキストをペアごとに検査し、重なり・枠外へのはみ出しなし。PCで目視、スマートフォン幅でページ全体の横はみ出しなし。
- ローカルリンクとSVGの既存2テスト成功。


## GNNの段階表示改訂（2026-09-12）

- 26ページの再生成、既存のリンク・HTML/SVG検査2件、JavaScript構文検査が成功。
- GATの手計算との一致、近隣の列挙順に対する不変性、別headの計算を検証する3件のテストが成功。
- 操作用コードを模擬DOMとタイマーで実行し、4教材の戻る／次へ、直接選択、再生、終端停止、再開、一時停止、全段階表示を確認。
- 今回のブラウザ表示確認は、ブラウザのURLポリシーがローカルファイルを拒否したため未実施。改訂後のPC・スマートフォンでの見た目と実ブラウザの操作は未確認。過去の表示確認結果とは区別する。

## Transformerの導入・表現更新の改訂（2026-09-12）

- RNNの順次処理と長い情報経路 → Transformerの設計思想 → 文脈に応じた表現更新 → 埋め込みと位置 → Q・K・V → ブロック → 応用・学習と生成の順に改訂。本文は`source/transformer.py`へ分離。
- 既存の「一つのqueryが集める情報」のスライダーと対応する処理を削除。7語の点が入力・第1〜3層を通って移動する図を追加。層の直接選択、前後送り、再生・一時停止・最初へ戻す操作を提供。実モデルの測定結果ではない模式図と明記。
- PCブラウザで全層の切り替え、7点の移動先、最終層で次ボタンが無効になること、再生が第3層で停止すること、一時停止・リセットを確認。自動再生はせず、動きを減らす設定では補間を省く。
- 新規静止図のSVGテキストを検査し、ラベル同士の重なりと図外へのはみ出しなし。ブロック図のラベルと接続線が重なる箇所は、ラベルを下の凡例へ移動。
- 幅390pxのブラウザで点群とボタン・説明が収まることを目視確認。ページ幅も390pxで、ページ全体の横はみ出しなし。
- 全26ページの再生成、JavaScriptの構文確認、既存のローカルリンク・素材・ID・SVG等の2テストが成功。

## モデルの背景説明の補足（2026-09-12）

- モデル10系列の説明を確認し、AE・VAE、GAN、Normalizing Flow、Diffusion、Flow Matching、GNNとGCN・GAT・GraphSAGEに、解決したい課題から設計へつながる説明を追加。既存のCNN・RNN・LSTM・Transformerの導入は維持し、ResNet・U-Net・GRU・ViTの背景も各説明内に補足。
- SSMページに重複していたRNNの導入・図を修正し、状態空間モデルの背景、長い系列に使う目的、線形SSMの更新式へつなぐ構成に変更。Mambaの入力に応じた選択の説明は維持。S4と拡散モデルの初期研究の参照先を追加。
- HTML全26ページと検索索引を再生成。生成ログは139節・87,495文字（図中文字・数式・表を含む抽出値）。
- 既存のtest_landscape.pyの2件、test_gnn_calculation.pyの3件が成功。ページ内ID、ローカルリンク・素材、SVG、GNNの計算を確認。生成後のSSM本文も抽出して確認。今回の本文追記についてブラウザでの目視確認は未実施。


## GNNの図中心の説明への再改訂（2026-09-12）

- 段階選択を削除。ノード内の変更前後の値と、横の計算枠を常時表示。計算ごとに目的文を追加。
- 数値表示のアニメーションについて、開始・中間・正確な最終値・再実行・動きの低減を模擬DOMで検証。
- GATの集約図をSVGから画像化して確認し、説明文と辺ラベルの重なりを修正。ページ全体の実ブラウザ表示は未確認。
- 数値計算3件と既存の教材検査2件、再生成・JavaScript構文検査が成功。

## Transformer図の再調整（2026-09-12）

入力の埋め込み・位置情報を表現更新より前へ移動。成分ごとの加算と出力点群を作図。文章→ベクトル化→3層の選択式に変更し、再生と前後送りを削除。Attentionのスコア図と重心図を分離し、後者を重み付き和の説明の下へ移した。移動矢印はliveから重心への1本だけ。CSSと操作処理は内容に応じたURLで更新する。

## GPT生成までの説明（2026-09-12）
背景→全体構造→内部計算→GPT（入力の準備→表現更新→生成）へ再配置。ブロック図を縦方向の単一図へ統合し、層の表記を上付き(ℓ−1)/(ℓ)、位置を下付きに変更。GPTの入力はwithまでの5語。N層でwithのみ強調し、出力は全結合層→softmax→語彙確率（説明用）を表示。ブラウザでN層の強調・出力切り替え・構造図を確認。

## 図の縮小とベクトル変形（2026-09-12）
Attentionの図を最大高さ390pxに制限。文章→数値ベクトル→空間上の点を段階表示。出力は同じwith要素を移動し、丸から数値入りの四角に変形して全結合層・softmaxへ接続。途中で別の層を選ぶと待機中の遷移を取り消す。ブラウザで中間数値、最終形、確率表示と図の高さ390pxを確認。

## AE・VAEの説明と図の改訂（2026-09-12）

AEのラベルなし特徴学習、VAEの潜在分布と生成の動機をMNISTで説明。両端に実際のMNIST画像を置き、AEはニューロンの圧縮構造、VAEは中央に確率密度の山とサンプリングを表示。復元目標の模式表示であることを明記。ローカルHTMLをブラウザで描画し両図を目視確認、図中文字の重複なし、390px画面でページ全体の横はみ出しなし。教材のリンク・素材検証2件成功。

## SSM・Mambaを概念から読む構成へ改訂（2026-09-13）

- 指定されたMaarten GrootendorstのVisual Guideを構成の参考にし、本文の正本を`source/ssm.py`へ分離。猫の名前を覚える例→状態の意味→引き継ぎ・書き込み・読み出し→S4→選択的SSM→Mamba-1ブロックへ構成し直した。
- 独自の静止図7点で記憶の持ち方、状態の意味、数値の減衰、内容に応じた選択、ブロックの分岐と残差を説明。数式・離散化・parallel scanは4つの折りたたみ補足へ配置。実測と模式例を区別。
- 技術面はMamba論文と著者実装も参照。固定係数でも入力によって状態は変わること、Aと離散化後のĀの違い、KVキャッシュ、scanが時間順序を保存することを明記。
- HTMLと検索索引の再生成成功。既存のローカルリンク・素材・ID・SVG検証2件成功。ブラウザでMambaの比較図・ブロック図、補足の開閉と数式表示を確認。全7図のテキスト矩形間の重なりなし。
- 390px幅で本文を目視し、ページ全体の横はみ出しなし（広い図は既存の横スクロール）。B3による理解度の試読は未実施。

## Normalizing Flowの図の改訂（2026-09-13）

抽象的な可逆層の箱を、実際に計算した2次元等密度線の4段階の変形に変更。拡大・非線形変形・座標交換と逆演算を併記。密度は正規分布の曲線を同じ尺度で比較。Couplingは数値の乗加算と減除算で図示。20点で逆変換による復元を検証。3図をブラウザで目視確認し、文字の重なりなし、390pxでページの横はみ出しなし。教材検証2件成功。

### 2026-09-13：Transformer補足のViT・VLM

- ViTを独立した`vit.html`へ移し、Transformerの直後に配置。VLMの画像エンコーダ→接続層→LLMの図解を追加。
- 27ページを再生成。既存の`test_landscape.py`の2件が成功（リンク・ID・素材）。
- ブラウザでパッチ画像の切り出し、CLS分類図、VLM入力経路の配置を目視確認。VLM図は確認時675×540px。

## SSM：出典に沿った図解と状態更新アニメーション（2026-09-13）

- 全要素間のAttentionのO(N²)と、状態次元を固定した再帰SSMのO(N)を図中に明示。連続・離散SSMのA・B・Cの式を導入本文へ移動。
- 独自の猫と物体の例を削除。HiPPOの著者解説と原論文を参照し、多項式基底・係数による圧縮、LegSの全履歴一様重み、LegTの固定窓、LagTの指数重みを説明。原図2点を出典付きで掲載。
- 畳み込みは再帰式を展開した図でカーネルと入力の積和を表示し、並列学習とFFTを使う目的を説明。選択的SSMはMamba Figure 2のSelective CopyingとFigure 1の入力依存の係数に基づき再構成。原図も補足に掲載。
- アニメーションはhとx→ĀhとB̄x→新しいh→y→hを左・yを上・新しいxを下から、の5段階×3時刻。再生・一時停止・段階送り・リセット、完了時停止、非表示時の停止、動きの低減に対応。JavaScriptなしでも更新図・式は読める。
- `node tests/test_ssm_animation.cjs`で実装のフレーム時計を制御し、引き継ぎ途中の座標、停止・再開、リセット、完了・再実行、非表示時停止、動きの低減を検証。実ブラウザでも15段階の状態・出力ラベル、履歴の蓄積、完了時のh₃の位置と出力を照合。

## Diffusion更新（2026-09-13）

- 箱だけの順方向図と8×8数字の図を、写真風の猫による段階図に置換。生成側もノイズから猫が現れる5段階へ変更。AI生成の説明素材であり実測中間出力ではないことを明記。
- 学習時の正解ε・損失・逆伝播、U-Netのスキップ接続、DiTのパッチ処理、文章条件のcross-attentionを図示。条件付き学習・生成、CFGの条件ドロップと予測の合成を追加。
- HTML再生成、`test_landscape.py`の2テスト、Diffusion内SVGのXML・参照先検証に合格。
- ブラウザで順方向、学習、U-Net、DiT、条件入力、生成の各図を目視確認。猫画像、矢印、文字の表示を確認。
- 画像の最終プロンプトと内蔵ツール利用の記録は`assets/README.md`に保存。

## Flow Matchingの図解更新（2026-09-13）

- 「1点＝1サンプル」「分布＝値の出やすさ」から導入し、Normalizing Flowの変形と接続。点群の3時刻、速度場、学習用の直線ペア、速度回帰、生成の1歩の5図を追加。
- 速度場と直線ペアは再生・一時停止・一歩進む・リセット・時刻指定に対応。操作部分はページ専用の `assets/fm.js`。自動再生せず、動きを減らす設定では再生時に終点を表示する。JavaScriptなしでも静止図を表示。
- 図の変形は説明用の既知の式であり学習結果ではない。学習時のペアと生成時の経路の違い、画像では1点が1枚に対応することを明記。
- HTML再生成と既存の文書リンク・素材検査が成功。操作ロジックは模擬DOMで再生・停止・時刻指定・終了・終点座標・動きの低減を確認。今回の変更に対するブラウザでの見た目の検証と学習者による試読は未実施。
- SSM専用の操作処理は`assets/ssm.js`へ分離し、このページだけで読み込む。独立したテストも同じ配布コードを実行する。
- 最終版をブラウザで再読み込みし、初期化・自動再生完了・原図の表示を確認。エラーログなし。静止SVGの文字同士の重なりなし。390px幅ではページ全体の横はみ出しなし（式と広い図は横スクロール）。

### 2026-09-13：Encoder・Decoder比較

因果マスク図を構造図に置換。Self/Cross-Attentionの横並びQKV図を追加。ブラウザで両図を確認し、中央の矢印と重なる補助ラベルを削除。Transformerページのリンク・ID・SVGを検証。全体検査では変更範囲外のvision.html→research.html#sam2にリンク切れがあり、未修正。

## 2026-09-13：画像のタスクの改訂

- 分類・回帰・検出・セグメンテーション・深度・姿勢を独立節にし、各タスクに図と用途の説明を追加。
- セグメンテーション3種類は同じ車の輪郭を共有し、個体IDと背景の扱いを図示。深度には著者公開の同一写真と推定結果を使用。
- 「タスク・検索語」を「タスク」へ変更。「正解との比べ方も変わる」を削除。
- 画像ページから既存の研究例3件へ直接リンク。研究紹介の本文は変更せず、リンク先のアンカーのみ追加。
- HTML再生成と既存の教材検証2件を実施。1440px・390pxで表示、画像読み込み、横はみ出し、研究例への遷移を確認。

- 追記：各画像タスクに複数の図例を追加。分類ラベルを「車」「チューリップ」として表示。深度は街路と自転車の2組。狭い画面では入出力を縦に並べる。追加後も既存の教材検証2件に合格。

### 目的・学習手順・生成のコマ送りの追補

冒頭に画像生成の目的、ネットワーク構造ではなく学習方法であること、学習中の数値積分の負担を避ける動機を追加。学習問題作成は4コマ、生成は予測と移動を交互に示す8コマへ変更。速度の2成分の誤差と、出力バイアスの勾配更新の数値例を追加した。ブラウザのローカルプレビューで生成の全8コマ、終了時のボタン無効化、リセット、自動再生を確認し、表示も確認した。文書リンク検査と再生成が成功。コマは説明用の固定速度関数をEuler法で計算したもので、学習済みモデルの出力ではない。

## 2026-09-13：タスクと実問題の改訂

- 29ページを再生成。意思決定・組合せ最適化・科学を独立ページとして目次、前後リンク、検索へ反映。
- `tests/test_landscape.py` の2検査を通過。全ページの内部リンク・アンカー、ローカル素材、重複ID、SVGを確認。
- TSPは同一6都市の全順列から最短経路を算出。長い例11.01、最短6.92（任意単位、丸め後）。20都市の重複を除く経路数も確認。
- ガントチャートの両予定で工程順序・機械の非重複・終了時刻9分／5分を数値検査。
- 論文のp.4を描画し、Figure 2の切り出しと凡例を目視確認。工程と制御のSVGを静止画像へ描画して確認。
- 今回のローカルHTMLのブラウザ表示はURL安全ポリシーで拒否されたため未確認。PC・スマートフォンの実ブラウザ表示を確認済みとはしない。追加の図は狭い画面で縦並びになるCSSを採用。

## 2026-09-13：同じデータ集合の2件へ整理

- 「例1・例2」とデータごとの枠を廃止し、入力画像と出力の共通2列にまとめた。
- 分類・検出・分割は同じ教材用街路集合の2枚を共有。姿勢も同じ作図規則の2件へ統一。
- 回帰をUTKFaceの顔画像2枚と年齢の数値に変更。年齢ラベルを用いた表示であり、新しいモデル推論ではないと記載。
- 深度の街路・自転車の入力写真と深度マップ4枚を維持。
- 再生成後、既存の教材検証2件に合格。1440pxと390pxでページの横はみ出し・画像欠損なし。「例」を含む追加見出しが残っていないことを確認。分類・回帰・分割・深度を画面で確認。

### Flow Matchingの説明の再構成：個別の経路と分布の対応

汎用的な箱図、1ペアの問題作成の4コマ、逆伝播・バイアス更新の復習、手で定めた無関係な速度の生成例を撤去。正規ノイズから2成分の正規混合分布へ移す一つの例に統一した。独立なペアの経路、位置・時刻で条件付けた平均速度、CFMの損失の意味、連続の式による密度の対応を説明する。本文は `source/fm_lesson.py` に分離。

比較アニメーションは、左がペアの直線、右が解析的に計算した条件付き平均速度のODE。21コマ、160点、RK4刻み0.0025。ネットワークの訓練結果とは表示しない。赤ペアは行き先の違いを追う固定例として明記。`test_flow_matching.py` で連続の式の残差と、生成終点の混合分布の累積確率・直交成分の変換を検証し成功。既存の文書検査も成功。
ブラウザでコマ送り、自動再生の21コマ完走、最終コマでの送りボタンの無効化を確認。最終図全体を表示し、両側の二つの山と赤点の異なる行き先を視覚的に確認した。

## 2026-09-13：分類を犬・猫へ、姿勢を論文図へ変更

- 分類はOxford-IIIT Petの犬・猫各1枚に変更。共通の入力／ラベル列、囲みなしの表示を維持。種別ラベルを用いた表示であることを明記。
- 姿勢推定の独自模式図を撤去し、BlazePose原論文v1 Figure 6の2パネルへ差し替え。論文HTMLで図番号と結果図であることを確認し、画像と出典・読み方を掲載。
- 年齢推定と深度の写真・マップを維持。再生成と既存の教材検証2件に合格。1440px・390px表示で画像欠損とページの横はみ出しなし。犬・猫と論文図を目視確認。

## 2026-09-13 意思決定ページの再構成

- 組合せ最適化を意思決定ページ内へ統合。科学は別ページ。28ページを再生成し、旧optimization.htmlは移動案内に変更。
- エージェント・環境・行動・観測と報酬の一般的な図へ変更。SVGの拡大原因だった未適用のペア用CSSを、意思決定ページに限定して追加。
- 同一10都市で長い／短い経路を比較し、都市を一つずつ選ぶ途中経過を追加。両方が全都市の順列で、表示距離が実座標から計算されることを確認。最短性は主張しない。
- 既存のリンク・アンカー・素材の2検査が通過。TSP、生成過程、ガントチャート、RLループを静止画像へ描画して視認。ブラウザ表示は既出のローカルURL制限により未検証。

### 科学ページの改訂（2026-09-13）

- 6タスクを独立節に分け、目的・入力と出力・図の読み方・検証点を追加。AlphaFold2、RFdiffusion、FNO、GraphCast、scGenの原論文図5点を本文と対応させた。出典・引用版・切り出し範囲を素材記録に追記。
- 冒頭の2枚のSVGが幅いっぱいに拡大される問題を、科学ページ専用の2列レイアウトと幅上限340 pxで修正。論文図は表示高480 px以内、クリックで拡大。
- ブラウザ1280×720で冒頭の比較図とAlphaFoldの論文図を目視確認。390×844で冒頭の縦配置を確認し、ページ幅390 px／スクロール幅390 px、各パネル幅334 px・高さ約275 pxを測定。ページ全体の横はみ出しなし。
- HTMLを再生成し、`python3 -m unittest discover -s tests -p test_landscape.py`の2テスト成功（内部リンク・同梱素材・SVG等）。原論文HTML内の参照アンカーも照合。

## 動画・3Dの拡充（2026-09-13）

- RAFTのFigure 1（v3 PDF p.2）、ORB-SLAM3のFigure 1・6（v2 PDF p.5・14）をページ描画で目視照合し、引用領域を切り出し。GenieのFigure 1は論文HTMLの原画像と図注を照合。出典・加工・版は`assets/README.md`へ記録。
- `source/spatial.py`と`source/spatial_generation.py`を追加。オプティカルフローの数値例、SLAMの部屋の例、3D表現比較、SoraとGenieの入出力・生成の流れを作図。図の値を実測結果と区別。
- ビルド後の`python3 -m unittest discover -s tests -p test_landscape.py`の2テストが通過。全ページのローカルリンク、アンカー、素材、重複ID、SVG素材を検証。
- Playwright Chromiumで1440 pxと390 pxの表示を確認。初回に具体例の2枚が過大表示されたため、このページ内で2列／モバイル1列のスタイルを追加し再確認。390 pxで文書幅390 px、具体例SVGの文字にviewBox外へのはみ出しなし。原論文図は拡大リンク付き。
- 新規画像4枚の読み込み、NeRFのローカル動画の再生（時間の進行0.8秒、全長4秒、エラーなし）を確認。ページのJavaScript実行エラーなし。
- ORB-SLAM3・SoraのYouTubeプレーヤーはHTTP表示から読み込み、各動画の正しいタイトルとプレビュー表示を確認。外部動画本編の連続再生までは自動操作で確認できていない。直接開くリンクも併記。
- HTMLの直接表示ではYouTube埋め込みがエラー153になることを確認したため、`file:`では埋め込みボタンを外部リンクの案内へ切り替える。初期表示の外部ネットワーク要求は0件。外部動画はオフライン素材とはしない。

## 2026-09-13 動画例と説明の短縮

- 例の順序をAlphaGo → ゲーム → 世界モデル → 組合せ最適化 → ロボットに変更し、生成された節順を検査。
- 組合せ最適化は比較図を中心に4節へ短縮。部分経路 → Transformer → 次の都市を追加、の図を描画して視認。
- DQN原論文のゲーム動画2本、DIAMONDのCS:GO動画、ORBITの並列シミュレーション動画を追加。既存RT-2と合わせ5本。全4ファイルを終端までデコード検査し、ポスターと後続フレームを確認。ORBITは約13秒、他は約68〜100秒。
- 既存のリンク・アンカー・素材の検査2件を通過。ブラウザ内再生はローカルURL制限により未検証。

### 科学ページ：入出力だけで読める再改訂（2026-09-13）

- 研究での使い道と実験・詳しい計算での確認を冒頭へ移し、各タスクは入出力・視覚例・用途1文を中心に簡略化。細胞応答予測をAlphaFold2の直後に配置。AlphaFold2の2024年ノーベル化学賞を追記。複雑な原論文のモデル構成図は除去。
- 物理はGNSの著者公開Water-3D動画へ変更。ブラウザで再生し、readyState=4、paused=false、長さ50.08秒を確認。画面のGround truth／Predictionを日本語キャプションで説明。
- 画像復元のぼけと気象の移動アニメーションの再生操作を確認。操作後にぼけ量・移動座標が変化し、ボタンが一時停止へ変わることを確認。ページを読み込んだ直後は静止状態。
- 1280×720で物理動画と画像復元、390×844でAlphaFold2と入出力対を目視確認。狭幅時のページ幅／スクロール幅は共に390 px。
- 再生成、JavaScript構文チェック、教材のリンク・素材・SVG検証2テストが成功。GraphCast公式動画は外部接続用の補足として掲載し、外部プレーヤー内の再生は未確認。気象の教材アニメーションはオフラインで動作。

### 原図・公開映像への統一後の確認（2026-09-13）

動画・3D内の自作図を全撤去し、本文内のインラインSVGが0件であることを確認。SAM 2・NeRF・Genieの追加原図を含む画像7枚が読み込める。1440 px／390 px表示を再確認し、動画を幅100%・高さ自動にして元映像が切れないよう修正。3DGS追加動画は全長9.9099秒、1秒以上の再生時間の進行とエラーなしを確認。既存の2件の教材検証テストも通過。以前の自作図に関する検証記録は履歴として残す。

### モデル構成図を除去した結果中心のページ（2026-09-13）

- SAM 2の追跡結果3枚、RAFT Figure 3、ORB-SLAM3 Figure 6、Genie Figure 1の全6画像がデコード成功。
- 構成図と外部プレーヤーを除去。iframe・外部動画読み込みボタンは0件。
- localhost上で3動画を再生し、再生位置の進行とMediaErrorなしを確認：NeRF 1.02秒 / 全4秒、3DGS 0.98秒 / 全9.9099秒、Sora 1.01秒 / 全59.966667秒。
- 390px幅でページ幅390px。Sora動画とSAM 2結果をスクリーンショットで目視確認。
- 再生成と既存のページ整合性テスト2件が成功。


### 自然言語の入出力・デモへの改訂（2026-09-13）

- 本文を`source/language.py`へ分離。文書分類・抽出・要約・翻訳・質問応答のタスクから入り、固定回答の指示切り替え、検索資料を変えるデモ、LLaVAの著者公開出力へつなぐ。
- LLaVAの引用原図を目視確認。公開図と教材用固定例・定型回答を明記。外部推論デモの稼働・操作は未確認。
- JavaScript構文チェック成功。簡易DOMによる動作検証で質問3通り×資料有無2通り×受付時刻2通りの12条件と、指示5通りを確認。
- HTML再生成成功。初回の章全体のリンク検証は別ページ`time-graphs.html`が参照する`assets/timeseries.js`の欠落で失敗（自然言語以外の並行変更箇所）。
- ブラウザによるローカルファイル表示はURLセキュリティポリシーで拒否。今回の変更のブラウザ表示・狭幅表示の目視確認は未実施。
- その後、`timeseries.js`が存在する状態で章全体のリンク・素材・SVG検証を再実行し、2テストとも成功。

## 2026-09-13：時系列のタスク別説明と操作デモ

- 画像・意思決定ページの具体例を先に見せる構成に合わせ、予測・異常検知・変化点・欠測補完を独立した節へ整理。
- 合成電力需要の2種類の基準予測、実測の表示／非表示、MAEを追加。ブラウザで同時刻予測3.7 kW、最終値予測27.9 kWへの切り替えを確認。
- 合成センサの一瞬の上昇・持続する上昇・正常の切り替えとしきい値操作を確認。持続上昇／10℃は12点、25℃は0点、正常／1℃は3点の警報。変化点推定アルゴリズムの実行結果とは区別して記述。
- PCと390px幅でグラフ・操作欄を確認。390px幅で文書全体の横はみ出しなし。JavaScript無効時向けの初期SVGと説明を同梱。
- HTML再生成、JavaScript構文検査、既存の静的教材検証2件を実施。B3による試読は未実施。

### 科学ページ：図の意味・物理の逆問題・降雨動画（2026-09-13）
- 3つの生命科学例の関係を追記。入力と出力の説明を図に統合。細胞応答はAlphaFold2の直後を維持。
- 逆問題を地震波による地下構造推定へ、気象をDGMRの研究公開映像へ変更。
- 科学ページの全ローカルリンク・素材・アンカー・インラインSVGを検証：成功。JavaScript構文検証：成功。
- ブラウザでAlphaFold2・RFdiffusionの左右対応を目視確認。DGMR動画は duration=11.34、readyState=4、paused=false、currentTime=8.35 の実再生を確認。逆問題の再生ボタンで波の位置と説明文が更新されることを確認。
- 幅390pxで細胞応答の縦配置を目視、横幅390pxで横はみ出しなし。
- 章全体の既存テストは2件中1件失敗：別ページ time-graphs.html が assets/timeseries.js を参照するが検証時点でファイル未作成。科学ページの参照は全件成功。別ページには変更を加えていない。

## 音声・音楽の更新（2026-09-13）

- 画像・意思決定ページを参考に、入力→出力の一覧から音声認識、音声合成、音楽生成、音源分離、音の表現、区間検出へ進む構成に変更。
- 保存済みTTSの速度比較、MusicGen著者公開結果2例、8秒の旋律・伴奏・混合の切り替えを追加。元文章・元音源と、実モデルの出力を区別して記載。
- `build.py`再生成、`test_landscape.py`の2件（リンク・素材・ID・SVG・既存タンパク質データ検証）、`node --check assets/audio.js`が成功。
- Playwright/Chromiumでfile URLから音声6要素のメタデータ読み込み、再生中の旋律→伴奏切り替え、再生位置の維持、一時停止中の切り替えを確認。JavaScriptエラーなし。
- 1440pxと390pxの画面を画像で確認。390pxでページ全体の横はみ出しなし。楽譜は既存の図と同じ横スクロール方式。
- 機械的な再生・表示の確認であり、人による聴感評価や学生の理解度評価は未実施。

### フレーム操作とRAFTの2画像（2026-09-13）

7フレームの順送り、戻るボタン、左右キー、端の無効状態、画像と拡大リンク2箇所の同期をブラウザで確認。全フレームの画像デコード成功。RAFTが元画像と推定結果の2枚のみであることを確認。390pxで横溢れなし。両セクションのスクリーンショットを目視確認。既存テスト2件成功。

### 科学3図の表示改善（2026-09-13）
- 玩具的なビーズ・パズル図を実予測座標の主鎖／論文の構造図へ変更。細胞応答は凡例つきヒートマップへ変更。
- ブラウザで3図を目視確認。RFdiffusionのSVG表示で周辺領域が見えたためclipPathを追加して修正、再確認済み。
- 章全体の既存テスト2件成功（先のtimeseries.js未作成による失敗は解消済み）。最終変更後のインラインSVG構文確認成功。

## 推薦の具体例を拡充（2026-09-13）

- Netflix・YouTube・Spotify・Amazonの公式説明を確認し、4つの独立した説明と「入力の手がかり → おすすめの順序」の教材用架空例を追加。公式出典は説明のそばに掲載。
- ニュース、SNS、求人、学習教材、旅行・飲食店の応用例、内容に基づく推薦と協調フィルタリング、履歴がない場合と評価の説明を追加。
- HTML再生成と既存の静的教材検証2件に成功。ブラウザで4つの例の存在と、Netflix例のPC／390px表示を目視確認。390px幅の文書全体に横はみ出しなし。


### LLMデモ削除とVLM・CLIPの自作図（2026-09-13）

- LLMの指示切り替えUIと対応するJavaScriptを削除。RAGは独立した検索デモとして維持し、起動条件を更新。
- VLMは著者原図から画像・質問→特徴→LLM→回答の教材独自図へ変更。CLIPは共通特徴空間と類似度表の2図を追加。原論文の手法説明を確認し、模式図・仮の数値を明示。
- 3図をSVGからPNGへレンダリングして文字・写真・配置を目視確認。SVGの矢印を線の方向へ向くように指定。
- HTML再生成、JavaScript構文検査、章全体のリンク・素材・SVG検証2件に成功。ブラウザ全体での表示確認は未実施。

### 音の数値表現の図と説明（2026-09-13）

- 同じ試聴用WAVから波形（全体・40 msの拡大）と実計算したスペクトログラムを追加。軸・色・旋律・伴奏・倍音の対応、変換の目的、位相と区間長の補足を記載。
- 「音の種類と、話した区間を調べる」の節とタスク一覧の該当行を削除。
- 図の日本語・凡例・軸を画像で確認。HTML再生成と既存文書検証2件が成功。Chromiumで1440px/390pxの画像読み込み、ページ横はみ出しなし、JavaScriptエラーなし、削除節が存在しないことを確認。


## XAIの再構成（2026-09-13）

- 表示名をXAIへ変更。導入、Shapley、単純な寄与度分解とIG、画像4手法、反実仮想2例、LLM3例の構成へ更新。既存URLを維持し、Transformerからの内部分析リンクを新しい節へ接続。
- ResNet-18の同一入力・同一クラスでSaliency、IG、LIME方式、Grad-CAMを計算。計算条件・数値データ・再現コードを同梱。
- IGの寄与合計10.466とスコア差10.517を照合（相対誤差約0.49%）。全配列が有限値であることを確認。LIME方式の局所近似は重み付きR²=0.829（近傍の当てはまりであり、忠実性全体の保証ではない）。
- 27ページを再生成し、`test_landscape.py`の2テストでローカルリンク・アンカー・素材・SVGを検証。
- ブラウザで画像6枚の読み込み、7個の説明SVGの文字の範囲、1280px幅の比較表示を確認。390px幅ではページの横はみ出しなし。大きな図は従来どおり図内で横スクロール可能。

### RAGのベクトル検索説明への改訂（2026-09-13）

- 語句一致・定型回答の操作デモを、階層のある大量の文書庫→チャンクのベクトル化→質問との類似度検索→本文と質問による回答の図と説明へ置換。原論文の要旨でdense vector indexと取得したpassageによる生成を確認。
- CLIPの「利用時は、候補文や画像を順位付けする」節と図内の利用説明を削除。
- HTML再生成、章のリンク・構造検証2件が成功。4図のSVG描画が成功し、RAG図を画像で確認。矢印属性の重複を解消し、注記と線の重なりを修正。ブラウザ上の確認は未実施。

### XAIの画像説明とLLM図の再構成（2026-09-13）

- LIME・Grad-CAMの独立した説明節を削除し、四つの画像の直下に手順・色・限界をまとめた。
- DiCEは著者公開GIFへ変更。LLMは算術の特徴群、詩の分岐、多言語の共通処理の3図。「説明を見たら、一度確かめる」を削除。
- 27ページを再生成し、landscapeの既存2テストが通過。ブラウザで算術・詩・DiCEを確認し、DiCE画像の読込、独立見出しの削除、ページ幅のはみ出しがないことを確認。3枚のLLM図の文字同士の重なり・図外へのはみ出しはなし。

### RAGをベクトルそのものの図へ変更（2026-09-13）

- フローチャートと準備・検索・回答の本文を撤去。文書アイコンと質問の吹き出しをベクトルへ変え、同じ始点に重ねた矢印から最近傍の文書Aを取り出す図に変更。チャンク分割の説明は削除。
- 2次元の単位ベクトルを角度35・95・155度、質問40度で作図。類似度は角度差のコサインと一致。SVGを画像化して配置を確認。HTML再生成と章検証2件が成功。

### SAM 2入力比較・Genie 3（2026-09-13）

7フレームについて元画像・マスク結果と各拡大リンクの同期、全画像のデコードを確認。見出しを統合し、本文に「選手」「見てください」がないことを確認。Genie 3のローカルWebMは再生位置1.307984秒、全長20.111秒、MediaErrorなし。SAM 2とGenie 3を目視確認。390px幅で横溢れなし。再生成と既存テスト2件が成功。

### 説明文と音声合成デモの調整（2026-09-13）

- 「聞いてみましょう」「注目してください」などの呼びかけを削除し、操作後の状態文も説明文に変更。
- TTSをTacotron 2と人の同一英文の録音の比較に置換。出典・元ファイルの対応は素材記録に保存。
- 音源分離の箱と矢印の図と重複した説明を削除。波形・スペクトログラムの導入に、モデルが音を数値で扱うことを追加。
- HTML再生成・既存リンク等の文書検証2件・音声JavaScript構文検証成功。Chromiumで新規2音声を含む全プレイヤーの読み込み、音源切り替え、PC/スマートフォンの横はみ出しなしを確認。

### 3D順序・関数図・動画抜粋（2026-09-13）

点群→3DGS→座標関数→NeRFの見出し順をブラウザで確認。点群画像と関数図を目視確認。全画像デコード成功、390px幅で横溢れなし。Genie 3レース動画は20.111秒、再生位置1.330423秒、MediaErrorなし。Soraの15秒抜粋も再生を確認。タスク表・比較表を削除。再生成と既存テスト2件成功。

### 3D導入の用途中心への改訂（2026-09-13）

導入の点群・NeRF模式図を実ブラウザで確認。390px幅で文書の横はみ出しなし。点群→3DGS→NeRFの順、本文のハッシュ説明撤去を確認。既存の文書・素材・アンカー検証2件が成功。

### SLAM実画像・Genie 3 MP4（2026-09-13）

SLAM実画像のdecodeと配置を確認。Genie 3はH.264 MP4へ変更し、ブラウザでcurrentTime 1.45秒への進行、duration 20.11秒、media errorなしを確認。ローカル素材・アンカー検証2件が成功。

## 2026-09-14 プロジェクト分離

01〜05から独立した `ml-landscape` に教材・生成コード・素材・専用テスト・設計資料を移動。上記の共通ポータルに関する記録は分離前のものです。現在は `index.html` を直接開きます。
