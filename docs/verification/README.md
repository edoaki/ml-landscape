# 検証記録

日付ごとのフォルダに、その時点の確認手順と結果（JSON・Markdown）を残しています。現在の構成や最新の結果を表すものではありません。

2026-09-24に、各フォルダのスクリーンショット（PNG 433枚、約79MB）をGitの管理から外しました。記録本文から参照している画像はGit履歴（コミット `f006e61`）から取り出せます。現在の画面は `npm run test:browser` で `test-results/browser/` に再生成できます。

記録中の `.venv/bin/python …` などのコマンドは、Node.js移行前の手順です。
