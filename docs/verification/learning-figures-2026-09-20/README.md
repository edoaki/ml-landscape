# 転移・継続学習の図の改稿

対象は transfer-learning と continual-learning の2ページ。製品検査の例に統一し、更新対象、少数例、メタ学習の二段階、共有部分への誤差、移行先データの利用時点、忘却と復習の分岐、予測・評価・更新の順序を図示した。

通常の全体ビルドは別ページ information-geometry の distributions 部品不足で停止したため、通常の build.py のレンダリングを2ページに限定して dist を生成した。他ページへのリンクは既存 dist を参照。全54ページの検証を通したという意味ではない。

Chromiumで対象2ページを1440px・390px、JavaScript無効で確認。各図をスクリーンショットに保存し、横はみ出しがないことを確認。代表画像を目視し、文字と図形の配置を確認した。初学者による試読は未実施。

継続学習の段階切り替えを同時比較に置き換えたため、check_expansion.py の旧ステッパー対象から除外した。
