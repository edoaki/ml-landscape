# Attentionの効率化の追記（2026-09-20）

「圧縮・軽量な適応」に疎なAttentionの節を追加。長文質問応答を例に、BigBirdのlocal/global/random、Longformer、FlashAttentionとの違いを説明した。Transformerの計算量の節からリンクし、収録計画も更新した。出典の確認範囲はページのsources.ymlに記録。

- ビルド：54ページ成功、リンク・ID・素材の検証成功。
- 既存検証：test_content.pyの4件、test_landscape.pyの2件が成功。
- Chromiumのfile://表示で1280pxと390pxを確認。JavaScript無効でも図を表示でき、図は各パネル内に収まる。最初に見つかった共通SVG最小幅によるはみ出しをページ専用CSSで修正した。画像は修正後の表示。
- 本文だけから「長文で全組合せの計算が増える→参照先を絞る→遠くの情報を渡す経路も残す→精度と実測時間を確認する」を説明できることを制作側で確認。O(n)の条件、参照を省略しないFlashAttentionとの違いも確認。初学者の試読は未実施。

## Longformer・FlashAttentionの説明を独立（同日）

比較を主題とした小節と表を、それぞれの目的・具体例・仕組み・計算量を説明する小節へ変更。Longformerでは質問と文書の局所／全体の参照、FlashAttentionではHBMとSRAM・タイリング・softmaxの補正・学習時の再計算を説明した。原論文の該当節を確認し、出典の検証記録を更新。ビルド54ページとリンク・ID・素材の検証が成功し、出力HTMLの両節アンカーも確認した。本文のみで各手法の目的と処理の流れを説明できることを制作側で確認。

## 図と短い説明へ整理（同日）

LongformerとFlashAttentionを、それぞれ短い導入・専用の模式図・短い補足へ変更。詳細な手順と重複する評価説明を削除。独自SVG図で局所／全体の参照と、メモリ上の中間行列／小分けの計算を説明。ビルド54ページのリンク・ID・素材検証成功。Chromiumで1280pxと390px、JavaScript無効の表示と図のはみ出しなしを確認し、生成画像を目視確認した。

## 通常の処理との対比へ改訂（同日）

両節の本文と図を通常→各手法の順に統一。Longformerは同じ一語からの参照先、FlashAttentionは同じ入力から出力までの中間表の保存方法を対比。全体ビルドは変更範囲外のrepresentation-learningの未作成部品objectivesで停止したため、既存build.pyの処理をメモリ内で対象ページに限定して正本から生成。他ページは既存出力をリンク検証に使用。対象ページの生成、リンク・ID・素材検証、1280px／390pxのJavaScript無効表示と図のはみ出し確認を完了。

## FlashAttentionの操作アニメーション（同日）

3範囲・8コマの模式表示。通常の表の蓄積と読み戻し、FlashAttentionの作業領域再利用と途中出力の補正を表示。再生／一時停止・前へ・次へ・最初へを追加。自動再生なし、タブ非表示時停止、reduced-motionでは手動操作、JS無効時は静止説明。ビルド54ページ成功。Chromiumで全コマの保存ブロック数と完了状態、前後送り・リセット、時間経過での再生と停止、キーボード操作、reduced-motion、390px表示、JS無効の代替表示を検証。デスクトップとモバイル画像を目視確認。初回の操作検証は並行変更で導入されたdetailsを開かず失敗したため、開いて再検証。動きを減らす設定の検証は変更イベントの反映を待って確認した。

## ボタンを廃止して自動ループへ（同日）

850ms間隔・完了時1800msの自動ループへ変更。画面外／非表示タブ／閉じた補足内は停止し、reduced-motionでは完了図を静止表示。ボタンと逐次読み上げを削除。メモリ節約と移動待ち削減の意義を図の下へ固定表示。Longformer・FlashAttentionに種類と用途の一文を追加。ブラウザで全9状態・完了から先頭へのループ・ボタンなし・reduced-motion・スマホの横はみ出しなしを確認。アニメーション変更時は全体ビルド成功。説明追記後は別ページsafetyの未作成部品safety-pathで全体ビルドが止まったため、既存ビルド処理を対象ページに限定して生成し、既存出力を使ってリンクを検証。
