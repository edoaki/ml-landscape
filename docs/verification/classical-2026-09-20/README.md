# 古典的な機械学習ページの拡充（2026-09-20）

対象：`content/basics/classical/index.md`、同ページの `sources.yml` と図部品2点。

来客数予測と購入履歴分析を具体例に、線形・ロジスティック回帰、決定木、ランダムフォレストと勾配ブースティング、SVMとカーネル、k-means、PCA、行列・テンソル分解を説明。学習と利用、数値例、前処理と評価、研究の問いを追加。既存の節アンカーを保持。

決定木は来客数の具体例に置換。PCAは2特徴から1軸への投影をSVGで追加。図と数値は教材独自の架空例で、実データの学習結果ではない。公式資料と著者公開の総説を参照し、確認範囲をsources.ymlに記録。

## 検証

- `.venv/bin/python build.py`：58ページ生成。リンク・ID・素材参照検証に成功。
- `.venv/bin/python -m unittest discover -s tests -p test_content.py -v`：4件成功。
- Chromiumで対象ページを `file://`・オフラインで確認。幅1440pxと390px、390pxではJavaScript無効も確認。文書の横はみ出しとJavaScriptエラーなし。静的SVG2点、組版済み数式5点を確認。
- 決定木・PCA・行列分解・モバイル冒頭のスクリーンショットを目視確認。本文内の数式記法がそのまま表示された箇所を通常の文字表現へ修正し、再ビルド後に3条件で再確認。
- 執筆者による短い説明の試作で、各手法の目的・入力・中心の工夫・出力と、SVM／カーネル、クラスタリング／PCA、未観測／ゼロの区別を本文だけで説明できるか確認。読者による試読は未実施。

表示検査の結果は `browser-results.json`、確認画像は同フォルダに保存。公開・デプロイは行っていない。
