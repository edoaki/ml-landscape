# 頑健性とドメイン汎化の改稿（2026-09-20）

動作しない明るさスライダーを静的な比較図に置換。原因はページの scripts が空で、共通テンプレートも assets/app.js を読み込まないこと。単なる画像の明るさ変更は性能評価を伴わないため、製品検査を例にした説明図へ変更した。

図は計5点：撮影条件の比較、背景への依存、ドメインを分けた評価設計、ラベルの根拠を残すデータ拡張、平均値に隠れる条件別精度。全て教材用の模式図で、評価の数値は架空。本文と図から、頑健性の目的、ドメイン汎化と適応の違い、拡張の制約、条件別評価の必要性を説明できることを制作側で確認。読者による試読は未実施。

Chromiumで file:// のページを幅1440pxと390pxで確認。図5点、スライダーなし、JSエラーなし、ページ横はみ出しなし、SVG文字のviewBox外はみ出しなし。幅390pxではJS無効でも同じ内容が読める。広い図は図内の横スクロールを使用。全5図のスクリーンショットを目視確認。

既存 test_landscape.py の2テスト成功（リンク・素材・ID・SVG等）。全体ビルドは、別ページ information-geometry が未作成の components/distributions.html を参照しているため停止した。今回のページは既存HTMLの共通枠を使い、正本のMarkdownとコンポーネントを render_content.render で個別に再生成し、本文目次も更新した。他ページのソースは変更していない。
