# 旧Python制作コード（保管用）

2026-09-22に開発環境をNode.js＋npmへ統一しました。このフォルダは、既存素材の計算条件と過去の制作・検証手順を確認するための記録です。現行のビルド・テストからは呼び出しません。実行環境の維持は対象外です。

元の配置をこのフォルダ以下に保持しています。過去の制作記録にある `source/*.py`、`scripts/*.py`、`tests/*.py`、`build.py` はここを参照してください。旧コード内の相対パスは当時の配置を前提とするため、そのまま実行するためのものではありません。

現在の開発手順は [プロジェクトREADME](../../README.md) にあります。本文は `content/`、図や操作部品は各ページの `components/`、保存済みの素材は `media/` と `assets/` が正本です。GATとFlow Matchingの数値検証は `scripts/example-calculations.mjs` とNode.jsテストへ移行しました。
